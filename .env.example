PORT=8080

# Optional: when set, the backend uses MongoDB instead of the local JSON fallback.
MONGODB_URI=mongodb://127.0.0.1:27017/englishbuddy

# Required for live AI conversation and feedback.
OPENAI_API_KEY=your_openai_api_key

# Optional model override for AI practice.
OPENAI_MODEL=gpt-4o-mini
